name: Call Reusable Workflow Demo

on:
  workflow_dispatch:

jobs:
  call-reusable:
    uses: ./.github/workflows/reusable-workflow.yml
    with:
      environment: staging
    secrets:
      API_KEY: ${{ secrets.MY_TEST_SECRET }}
