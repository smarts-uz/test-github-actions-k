name: Reusable Workflow Demo

on:
  workflow_call: # Makes it reusable
    inputs:
      environment:
        description: 'Target environment name'
        required: true
        type: string
    secrets:
      API_KEY:
        required: false

jobs:
  print-info:
    runs-on: self-hosted
    steps:
      - name: Print Info
        shell: powershell
        run: |
          Write-Host "=== Running Reusable Workflow ==="
          Write-Host "Environment input: ${{ inputs.environment }}"
          Write-Host "Repository: $env:GITHUB_REPOSITORY"

      - name: Show Secret (if provided)
        if: ${{ secrets.MY_TEST_SECRET != '' }}
        shell: powershell
        run: |
          Write-Host "MY_TEST_SECRET is provided (masked in logs): $env:MY_TEST_SECRET"
        env:
          MY_TEST_SECRET: ${{ secrets.MY_TEST_SECRET }}
